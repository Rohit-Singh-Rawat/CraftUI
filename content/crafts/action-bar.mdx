---
title: Action Bar
category: Navigation
description: A versatile floating action bar with dock and contextual modes - perfect for navigation, selections, and notifications
---

# Action Bar

A versatile floating action bar component with two distinct modes: **dock** (persistent navigation) and **contextual** (conditional display). Supports 8 different positions, smooth animations, and fully responsive layouts. Perfect for mobile navigation, bulk actions, shopping carts, and contextual notifications.

## Craft Story

I kept saving examples of floating action bars from various apps that really helped with action-based interactions. They were perfect for bulk selections, shopping carts, and contextual notifications. But when I went looking for something similar in component libraries, I couldn't find anything that fit the bill.

So I decided to build my own. While making it, I thought about making it as reusable as possible - supporting different positions, dock mode for persistent navigation, and contextual mode for conditional actions. The result is something flexible enough to handle all those saved references and more.

<DependencySection
	dependencies={[
		{
			name: 'lucide-react',
			href: 'https://lucide.dev/',
		},
		{
			name: 'class-variance-authority',
			href: 'https://cva.style/',
		},
	]}
/>

## How to Use

<CodeBlock
	code={`import {
	ActionBarProvider,
	ActionBar,
	ActionBarHeader,
	ActionBarTitle,
	ActionBarActions,
	ActionBarClose,
} from '@/craft/components/action-bar';
import { Button } from '@/components/ui/button';
import { useState } from 'react';

function MyComponent() {
const [isOpen, setIsOpen] = useState(false);

    return (
    	<ActionBarProvider
    		mode='contextual'
    		open={isOpen}
    		onOpenChange={setIsOpen}
    	>
    		<ActionBar>
    			<ActionBarHeader>
    				<ActionBarTitle>2 items selected</ActionBarTitle>
    			</ActionBarHeader>
    			<ActionBarActions>
    				<Button size='sm'>Delete</Button>
    				<ActionBarClose />
    			</ActionBarActions>
    		</ActionBar>
    	</ActionBarProvider>
    );

}`}
language='tsx'
/>

## Props

### ActionBarProvider

<PropsTable>
	<PropsTableRow
		prop='mode'
		description='Display mode: "dock" for persistent navigation or "contextual" for conditional display'
		defaultValue='"contextual"'
	/>
	<PropsTableRow
		prop='position'
		description='Position of the action bar: "top", "bottom", "left", "right", "top-left", "top-right", "bottom-left", "bottom-right"'
		defaultValue='"bottom"'
	/>
	<PropsTableRow
		prop='open'
		description='Controlled open state (only for contextual mode)'
		defaultValue='-'
	/>
	<PropsTableRow
		prop='onOpenChange'
		description='Callback when open state changes (only for contextual mode)'
		defaultValue='-'
	/>
	<PropsTableRow
		prop='defaultOpen'
		description='Default open state for uncontrolled (only for contextual mode)'
		defaultValue='false'
	/>
</PropsTable>

### ActionBar

<PropsTable>
	<PropsTableRow
		prop='size'
		description='Size variant: "sm", "md", or "lg"'
		defaultValue='"md"'
	/>
	<PropsTableRow
		prop='position'
		description='Position override (inherits from provider if not specified)'
		defaultValue='-'
	/>
	<PropsTableRow
		prop='animationDelay'
		description='Animation delay in milliseconds (only for contextual mode)'
		defaultValue='100'
	/>
	<PropsTableRow
		prop='offset'
		description='Offset from the edge in pixels. For top/bottom: affects vertical spacing. For left/right: affects horizontal spacing. For corners: affects both axes'
		defaultValue='16'
	/>
	<PropsTableRow
		prop='maxWidth'
		description='Maximum width for horizontal positions (top, bottom, corners)'
		defaultValue='"640px"'
	/>
	<PropsTableRow
		prop='maxHeight'
		description='Maximum height for vertical positions (left, right)'
		defaultValue='"calc(100vh - 2rem)"'
	/>
	<PropsTableRow
		prop='className'
		description='Additional CSS classes'
		defaultValue='-'
	/>
</PropsTable>

### ActionBarClose

<PropsTable>
	<PropsTableRow
		prop='srText'
		description='Screen reader text for the close button'
		defaultValue='"Close"'
	/>
	<PropsTableRow
		prop='className'
		description='Additional CSS classes'
		defaultValue='-'
	/>
</PropsTable>

### Other Components

`ActionBarHeader`, `ActionBarTitle`, `ActionBarDescription`, `ActionBarActions`, `ActionBarContent`, `ActionBarLogo`, `ActionBarNav`, `ActionBarSeparator`, and `ActionBarTrigger` all accept standard HTML attributes and optional `className` prop.

## Attribution

Implementation inspired by [shadcn/ui](https://ui.shadcn.com) component patterns and modern mobile navigation interfaces.
