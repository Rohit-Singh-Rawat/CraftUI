---
title: Password Validation
category: Input
description: A better alternative to error messages - inline password validation badges that show requirements as you type
---

# Password Validation

An animated password validation component that provides real-time feedback on password strength with customizable validation rules. Built with Framer Motion for smooth animations and fully accessible with ARIA support.

## Craft Story

I saw a post by [@nickbakeddesign](https://x.com/nickbakeddesign) with a design concept for password validation using badges instead of error messages. This felt much better than traditional error lines that appear after form submission.

I implemented it with Motion for smooth interactions. Each badge animates in with a staggered delay, and the icons have a subtle zoom animation for immediate feedback.

The component automatically hides when all validations pass (unless you set `alwaysShow` to true), keeping the UI clean.

<DependencySection
	dependencies={[
		{
			name: 'motion',
			href: 'https://motion.dev/',
		},
		{
			name: 'lucide-react',
			href: 'https://lucide.dev/',
		},
		{
			name: 'Badge',
			href: 'https://ui.shadcn.com/docs/components/badge',
		},
	]}
/>

## How to Use

<CodeBlock
	code={`import { PasswordValidation } from '@/craft/components/password-validation';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { useState } from 'react';

function MyForm() {
const [password, setPassword] = useState('');

    return (
    	<div>
    		<Label htmlFor='password'>Password</Label>
    		<Input
    			id='password'
    			type='password'
    			value={password}
    			onChange={(e) => setPassword(e.target.value)}
    		/>
    		<PasswordValidation password={password} />
    	</div>
    );

}`}
language='tsx'
/>

## Props

<PropsTable>
	<PropsTableRow
		prop='password'
		description='The password string to validate'
	/>
	<PropsTableRow
		prop='rules'
		description='Array of custom validation rules. Each rule has a label and validator function'
		defaultValue='defaultPasswordRules'
	/>
	<PropsTableRow
		prop='error'
		description="Error object with optional message property (compatible with React Hook Form's FieldError)"
		defaultValue='-'
	/>
	<PropsTableRow
		prop='className'
		description='Additional CSS classes'
		defaultValue='-'
	/>
	<PropsTableRow
		prop='alwaysShow'
		description='Show validation even when all rules pass'
		defaultValue='false'
	/>
</PropsTable>

## Attribution

Design concept by [@nickbakeddesign](https://x.com/nickbakeddesign). Implementation inspired by [shadcn/ui](https://ui.shadcn.com) animation patterns.
